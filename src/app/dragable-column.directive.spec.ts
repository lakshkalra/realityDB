import { DragableColumnDirective } from './dragable-column.directive';

describe('DragableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new DragableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
